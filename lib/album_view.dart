import 'package:flutter/material.dart';

class AlbumView extends StatelessWidget {
  final Map data;

  AlbumView({Key? key, required this.data}) : super(key: key);

  @override
  Widget build(BuildContext context) {
  }
}
